<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://use.typekit.net/tml4vii.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="icon" type="image/png" href="/favicon.png"/>
    <title>Can I Vote?</title>
    {% include "partials/default-meta-tags.njk" %}
  </head>

  <body class="home ws-medium">

    {% include "partials/skip-to-content.njk" %}
    <link itemprop="thumbnailUrl" href="https://www.canivote.org.uk/img/can-i-vote.png">
    <span itemprop="thumbnail" itemscope="itemscope" itemtype="http://schema.org/ImageObject">
      <link itemprop="url" href="https://www.canivote.org.uk/img/can-i-vote.png">
    </span>

    <main class="container">
      
      <section class="main-content" id="can-i-vote">
        <h1 class="ws-bold"><img src="/img/can-i-vote-tick.svg" alt="Can I vote?"/></h1>

        <h2 class="ws-bold">{{words["main.not-just-british-citizens"]}}</h2>
        <form id="country-selector" method="get" action="/" autocomplete="off" type="search">
          <label class="screen-reader-text" for="id_country">
            {{words[language]['main.choose-country']}}
          </label>
          <select name="country" labelledby="id_country" id="id_country">
            <option value="" aria-hidden="true">{{words[language]['main.choose-country']}}
            </option>
            {% for country in countries %}
              <option value="{{ country.name | slug }}">{{ country.name }}</option>
            {% endfor %}
          </select>

          <input type="submit" value="{{words[language]['form.submit']}}">
        </label>
      </form>
    </section>
    {% include "partials/sharer.njk" %}
    {% include "partials/footer.njk" %}

  </main>
  <script src="https://cdn.jsdelivr.net/npm/css-vars-ponyfill@2"></script>
  <script src="/js/selector-interceptor.js"></script>

</body>

</html>